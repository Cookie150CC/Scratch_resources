

package pack;

/**
 * Brittain Cooke 10/10/17
 *CS 3310(Gupta) assignment 2
 *CharNode.Java:
 *  This class designates the instructions for creating and using a CharNode Object.
 *  The CharNode objects are used as the Building blocks for the CharList object/Linked List.
 */
public class CharNode {


    /**
     * Attributes / Fields
     */
    private char myData;
    private CharNode next;


    /**
     * CharNode():
     *Constructor function that creates a node with a single piece of data in it
     * @param pData
     */
    public CharNode(char pData){

        this.setMyData(pData);

    }

    /**
     * getMyData():
     *returns the char data that is stored in the CharNode's myData Attribute
     * @return
     */
    public char getMyData() {
        return this.myData;
    }


    /**
     * setMyData():
     *stores a char data in the CharNodes's myData Attribute
     * @param myData
     */
    public void setMyData(char myData) {
        this.myData = myData;
    }


    /**
     * getNext():
     returns the value of the next node pointer
     * @return
     */
    public CharNode getNext() {
        return this.next;
    }

    /**
     * setNext():
     *Assign a pointer to a different CharNode
     * @param next
     */
    public void setNext(CharNode next) {
        this.next = next;
    }
}
